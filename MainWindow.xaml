<Window x:Class="MoxaConfigApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Moxa Configuration Tool"
        Height="500"
        Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Margin="0,0,10,5" VerticalAlignment="Center">Montante:</Label>
        <TextBox x:Name="txtMontante" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" Width="300" />

        <Label Grid.Row="1" Grid.Column="0" Margin="0,0,10,5" VerticalAlignment="Center">Nuovo IP di management:</Label>
        <TextBox x:Name="txtIPAddress" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" Width="300" />

        <Label Grid.Row="2" Grid.Column="0" Margin="0,0,10,5" VerticalAlignment="Center">SSH Host (IP attuale):</Label>
        <TextBox x:Name="txtSSHHost" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" Width="300" />

        <Button x:Name="btnConfigure"
                Grid.Row="3"
                Grid.Column="1"
                Width="200"
                Margin="0,10,0,10"
                HorizontalAlignment="Left"
                Content="Avvia configurazione"
                Click="btnConfigure_Click" />

        <TextBox x:Name="txtLog"
                 Grid.Row="4"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 Margin="0"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="12" />
    </Grid>
</Window>
